Place publication PDF files in:

`public/publications/pdfs/`

Then reference the filename from `src/content/publications/index.ts` using the `pdfFile` field.

Example:

```ts
pdfFile: "paper-title.pdf"
```
